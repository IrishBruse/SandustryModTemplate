export const STATION_SIZE = 4;
/** World pixels per grid cell (`rendering.getGridMetrics().cellSize`). */
export const CELL_PX = 4;
/** Structure stamp size in cells (vanilla conveyor / smelter). */
export const TILE_CELLS = 4;
export const MAX_CARGO = 64;
export const MOVE_INTERVAL_MS = 160;
export const LOAD_PER_TICK = 8;
/** Loader intake drop animation (overlay ms). */
export const LOAD_DROP_MS = 85;
/** Cart sprite size vs one rail stamp. Overlay pixels do not follow Pixi zoom. */
export const CART_DRAW_SCALE = 1;
