Full documentation: [docs/modkit/README.md](../../docs/modkit/README.md).

This folder will probably become a published npm package if people start using it, so mods can depend on a versioned release instead of a copy or symlink.
